// var fs = require("fs");
// var input = fs.readFileSync("example.txt").toString(); ///dev/stdin

// const { arrayBuffer } = require("stream/consumers");

// const s = String(input[0]);
var maze;
// const arrType = Array.from(Array(typesN), function (v, k) {
//   return k + 1;
// });
function openDoor(i, j) {}
function solution(maze) {
  let res = 0;
  let key = 0;
  for (var i = 0; i <= maze.length[0]; i++) {
    for (var j = 0; j <= maze.length; i++) {
      //go left
      if(maze[i+1][0])
      //go down
    }
  }
  res = res % 1007;
  return res;
}
solution(maze);
